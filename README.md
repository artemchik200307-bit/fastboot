# FASTBOOT — главная страница рынка

## Изменения

- График полностью удалён с главной страницы.
- Главная страница показывает топ‑100 активных криптовалютных USDT-пар.
- Добавлен отдельный блок «Топ роста».
- Добавлен отдельный блок «Топ падения».
- При выборе инструмента обновляется карточка статистики рынка.
- Статистика включает цену, изменение, максимум, минимум, объёмы,
  средневзвешенную цену, цену открытия и количество сделок.
- Бегущая строка с котировками сохранена.
- Кнопки входа и регистрации сохранены.
- Рыночные данные обновляются автоматически каждую минуту.

## Обновление сайта

В приватном GitHub-репозитории `fastboot` замените:

- `index.html`
- `styles.css`
- `app.js`
- `README.md`

После Commit Render автоматически развернёт новую версию.


## FASTBOOT AI Trading Ecosystem

Внизу главной страницы добавлен информационный раздел о разрабатываемой
AI-экосистеме:

- AI Market Analysis
- AI News Analysis
- AI Smart Money System
- AI Risk Management
- AI Trading Assistant
- Institutional Analysis

Раздел честно помечен как находящийся в разработке. Roadmap не добавлялся.


## Личный кабинет

Добавлены:

- `dashboard.html`
- `dashboard.css`
- `dashboard.js`

Кнопка «Войти» на главной странице временно открывает демонстрационный личный
кабинет без регистрации.

В кабинете доступны интерфейсы:

- обзор;
- AI Trading Assistant;
- анализ рынка;
- анализ новостей;
- Smart Money;
- риск-калькулятор;
- избранные монеты;
- торговый журнал;
- история анализов;
- настройки профиля.

Регистрация, база данных и настоящий AI будут подключаться отдельными этапами.


## Account v2

Добавлена новая структура личного кабинета:

- Dashboard основного счёта;
- портфель, пополнение, вывод и обмен;
- AI Trading Bot с отдельным балансом;
- торговый терминал с графиком, стаканом и лентой сделок;
- анализ рынка без свечного графика;
- журнал ручных ордеров;
- страницы входа и регистрации;
- локальный тестовый аккаунт администратора.



Важно: авторизация и денежные операции пока работают только через localStorage
браузера. Это прототип интерфейса, а не защищённая серверная система.


## Terminal v3

- Удалены настройки риск-менеджмента из AI Assistant.
- Торговая страница переработана в полноэкранный биржевой терминал.
- Боковое меню автоматически скрывается в разделе торговли.
- Кнопка с домиком открывает боковое меню.
- Добавлены рыночная сводка, стакан, лента сделок, форма лимитного/рыночного ордера и процентные кнопки.


## Professional Terminal v4

Торговый раздел переработан в профессиональный терминал:

- полноэкранный режим;
- адаптивные пропорции;
- верхняя панель рыночных показателей;
- свечной график и объёмы;
- переключение таймфреймов;
- стакан с визуальной глубиной;
- лента последних сделок;
- Limit, Market и Stop интерфейсы;
- процентный ползунок размера ордера;
- TP/SL и Post Only в интерфейсе;
- отдельные кнопки Buy и Sell;
- вкладки позиций, открытых ордеров, истории и активов;
- панель баланса аккаунта.

Ордера остаются локальными, пока не подключён настоящий биржевой backend/API.


## Mobile Terminal v5

На смартфонах торговый раздел использует отдельную компоновку:

- верхнее мобильное меню рынков;
- выбранная пара и изменение цены;
- стакан слева;
- форма ордера справа;
- параметры маржи и плеча в интерфейсе;
- Limit / Market / Stop;
- процентный ползунок;
- проскальзывание, TP/SL и Reduce Only;
- крупные кнопки Buy/Long и Sell/Short;
- соотношение заявок покупателей и продавцов;
- нижние вкладки ордеров, позиций и ботов.

Компьютерная версия терминала остаётся прежней.


## Terminal v6 — compact desktop and top-100 search

Изменения:

- уменьшена высота графика на компьютере;
- убран отдельный блок последних сделок справа;
- рядом с графиком оставлен только расширенный стакан;
- сохранена форма размещения ордеров;
- добавлено окно выбора инструмента;
- список автоматически формируется из топ‑100 активных USDT-пар;
- добавлен быстрый поиск по тикеру и названию монеты;
- в списке отображаются цена и изменение за 24 часа;
- мобильная компоновка терминала сохранена.


## Mobile Terminal v7

Мобильный терминал упрощён:

- удалён верхний ряд USDⓢ-M / COIN-M / Options / Smart Money;
- удалена кнопка с тремя полосками;
- добавлена кнопка-домик для открытия бокового меню;
- поиск по топ‑100 монетам сохранён;
- удалены Cross, 10x и M;
- удалена ставка и обратный отсчёт;
- оставлены только Limit и Market;
- удалены Slippage и Reduce Only;
- TP/SL сохранён;
- снизу оставлены только Open Orders и Positions;
- стакан уменьшен;
- форма ордера остаётся справа от стакана;
- мобильный терминал помещается в высоту экрана без длинной прокрутки.


## Desktop Terminal v8

Исправления компьютерной версии:

- общий масштаб интерфейса увеличен примерно на 10%;
- верхняя строка информации о монете стала ниже и компактнее;
- график получил нормальную фиксированную высоту;
- стакан уменьшен и выровнен по высоте с графиком;
- форма ордера выровнена по высоте с графиком;
- исправлена отложенная инициализация графика после открытия раздела;
- добавлен надёжный ResizeObserver;
- график автоматически подгоняется под размеры контейнера;
- мобильные стили не изменялись.


## Terminal v9 — chart and orderbook fix

- Исправлена ошибка, из-за которой график и весь терминал не загружались.
- Причина: удалённый блок последних сделок всё ещё использовался JavaScript.
- Удалён лишний запрос последних сделок и обращение к отсутствующему элементу.
- Стакан теперь показывает ровно 10 заявок на продажу и 10 заявок на покупку.
- Заявки больше не обрезаются CSS.
- Добавлено более точное сообщение об ошибке загрузки рыночных данных.


## Mobile Terminal v10

- Мобильный стакан показывает ровно 5 заявок на продажу и 5 заявок на покупку.
- Стакан и форма ордера всегда расположены рядом: 43% / 57%.
- Страница терминала не прокручивается целиком.
- Форма ордера имеет собственную компактную прокрутку при необходимости.
- Снизу закреплены только «Открытые ордера» и «Позиции».
- Содержимое выбранной нижней вкладки показывается в фиксированной панели.
- Поиск по топ‑100 монетам доступен сверху.
- Размеры текста, полей и кнопок уменьшены и выровнены для мобильного экрана.


## Terminal v11 — realtime desktop

- Свечи обновляются автоматически через Binance WebSocket.
- Текущая цена и статистика 24ч обновляются без кнопки Refresh.
- Стакан обновляется в реальном времени.
- При обрыве соединения терминал автоматически переподключается.
- При смене монеты или таймфрейма старое WebSocket-соединение закрывается.
- На компьютере стакан показывает 8 заявок на продажу и 8 на покупку.
- Убран случайно отображавшийся большой мобильный домик.
- Верхняя строка информации стала компактнее.
- График стал шире, а форма ордера — немного уже.
- Стакан и форма ордера стоят вплотную друг к другу.


## Mobile Terminal v12 — corrected layout

- Исправлено перекрытие выбора монеты и стакана.
- Выбор монеты закреплён в компактной верхней панели.
- Стакан и форма ордера используют всю доступную высоту экрана.
- Удалён большой пустой блок истории под рабочей областью.
- Снизу оставлена только компактная панель «Открытые ордера / Позиции».
- Стакан показывает 5 заявок на продажу и 5 на покупку.
- Текущая цена находится по центру стакана.
- Увеличена читаемость текста стакана.
- Форма ордера остаётся справа и не переносится вниз.
- Размеры полей, текста и кнопок приведены к мобильному формату.


## Mobile Terminal v13

- Кнопка домика выровнена с выбором монеты.
- Домик оформлен синим цветом FASTBOOT.
- Процент в правом верхнем углу скрыт.
- Добавлена синяя кнопка открытия полноэкранного графика.
- Мобильный график поддерживает 1м, 5м, 15м, 1ч и 4ч.
- Переключатель TP/SL заменён на аккуратный синий switch.
- Текущий раздел личного кабинета сохраняется в localStorage.
- После обновления страницы раздел «Торговля» остаётся открытым.


## Mobile Terminal v14

- Выбор монеты поднят и выровнен с кнопкой домика и кнопкой графика.
- По нажатию «Открытые ордера» открывается нижняя выезжающая панель.
- По нажатию «Позиции» открывается панель позиций.
- Повторное нажатие на активную вкладку закрывает панель.
- При открытой панели рабочая область терминала автоматически уменьшается.
- Панель не увеличивает общую высоту страницы.


## Mobile Dashboard Scroll Fix v15

- Прокрутка работает во всех разделах личного кабинета.
- Блокировка прокрутки применяется только в разделе «Торговля».
- При выходе из торговли overflow сбрасывается.


## Mobile Scroll Container Fix v16

- На iPhone прокрутка обычных разделов выполняется внутри `.dashboard-main`.
- Это обходит сохранённую Safari блокировку прокрутки `body` после терминала.
- AI Assistant, Dashboard, Journal и Settings прокручиваются отдельно от меню.
- Торговый терминал по-прежнему остаётся полноэкранным и неподвижным.

## Supabase Auth Clean

- Тестовый локальный администратор удалён.
- Старая авторизация через localStorage удалена.
- Вход и регистрация работают только через Supabase Auth.
- Dashboard запускается только после проверки Supabase-сессии.
- Цикл переходов login/dashboard устранён.
- Выход выполняется через Supabase `signOut()`.
- Профиль загружается из таблицы `profiles`.
- Балансы загружаются из таблицы `wallets`.

`supabase-config.js` не включён в архив специально:
оставьте уже настроенный файл в корне GitHub-репозитория.


## Dashboard & Wallets v2

Добавлено:

- загрузка основного и AI-баланса из Supabase;
- заявки на пополнение и вывод;
- история заявок со статусами;
- безопасный перевод Spot → AI Bot;
- безопасный перевод AI Bot → Spot;
- атомарные SQL-функции с блокировкой строки кошелька;
- финансовые данные больше не сохраняются в localStorage;
- локальные ордера и настройки бота разделены по ID пользователя.

### Установка

1. Откройте Supabase → SQL Editor.
2. Выполните `supabase-dashboard-wallets.sql`.
3. Загрузите остальные файлы в GitHub с заменой.
4. Не удаляйте существующий `supabase-config.js`.


## Admin Panel v1
1. Выполните `supabase-admin-panel.sql` в Supabase SQL Editor.
2. Загрузите остальные файлы в GitHub с заменой.
3. Выйдите и войдите в аккаунт снова.


## USDT TRC20 Funding v1

Адрес SafePal: `TVwAj44gxbPFTDH3KifsmqjfCtF54tj4DC`

- Пополнение только USDT TRC20.
- QR-код и копирование адреса.
- Минимум 10 USDT.
- Обязательный уникальный TXID.
- Проверка TXID через Tronscan.
- Вывод только USDT TRC20.
- Обработка заявок через Admin Panel.

### Установка
1. Выполните `supabase-trc20-funding.sql` в Supabase SQL Editor.
2. Загрузите остальные файлы в GitHub с заменой.
3. Не заменяйте `supabase-config.js`.


## Complete Personal Cabinet v1

Добавлено:

- карточки основного и AI Bot баланса;
- суммы и количество ожидающих заявок;
- единая история пополнений, выводов и переводов;
- фильтры истории;
- ссылки на TXID в Tronscan;
- полная карточка аккаунта;
- безопасное изменение username через Supabase RPC;
- изменение пароля;
- выход со всех устройств;
- автоматическое обновление данных каждые 30 секунд;
- обновление после возвращения на вкладку.

### Установка

1. Выполните `supabase-complete-cabinet.sql` в Supabase SQL Editor.
2. Загрузите остальные файлы в GitHub с заменой.
3. Не заменяйте существующий `supabase-config.js`.


## AI Assistant v1

- Раздел остаётся с названием AI Assistant.
- Пользователь не видит ссылки на административную запись сделки.
- Администратор открывает и закрывает сделки в Admin Panel.
- При закрытии задаётся результат в процентах.
- PnL рассчитывается индивидуально от AI-баланса пользователя.
- История содержит пару, направление, цены, время, процент и сумму результата.

### Установка
1. Выполните `supabase-ai-assistant.sql` в Supabase SQL Editor.
2. Загрузите остальные файлы в GitHub с заменой.
3. Не заменяйте `supabase-config.js`.


## AI Assistant Fix 1.1

Исправлено:

- блок «Активные сделки» удалён;
- в AI Assistant остаются динамика и история;
- загрузка профиля и кошелька больше не зависит от AI-таблиц;
- email, FASTBOOT ID и балансы снова загружаются независимо;
- Admin Panel снова показывается для `profiles.role = 'admin'`;
- Admin Panel добавлен в список страниц;
- административные элементы получили безопасные проверки на существование;
- отсутствие или ошибка AI-таблиц больше не ломает весь Dashboard.

SQL повторно выполнять не нужно, если `supabase-ai-assistant.sql`
уже был успешно выполнен.


## Account Fix 1.2

- новый cache-busting для dashboard.js и dashboard.css;
- восстановлена независимая загрузка профиля и баланса;
- удалён повреждённый повторный обработчик кнопки «Домой»;
- ошибки AI-таблиц больше не ломают Dashboard;
- Admin Panel проверяет нормализованную роль admin.

SQL выполнять не нужно.


## Terminal and Account Fix 1.3

Исправлена ошибка:

`ReferenceError: loadTradingTerminal is not defined`

Также восстановлены:

- `formatPrice`;
- `formatQuantity`;
- загрузка графика терминала;
- загрузка стакана;
- 8 заявок на компьютере;
- 5 заявок на телефоне;
- новый номер версии JS/CSS против кэша.

SQL выполнять не нужно.


## Order Fix 1.4

Исправлена ошибка:

`ReferenceError: updateOrderCalculation is not defined`

Также восстановлены:

- `calculateOrderAmountFromPercent`;
- `placeLocalTerminalOrder`;
- расчёт суммы ордера и комиссии;
- процентный выбор количества;
- локальная история ордеров и сделок;
- защитные проверки элементов формы;
- новая версия JS/CSS против браузерного кэша.

SQL выполнять не нужно.


## No Terminal + Direct AI Trades v1.5

- раздел «Торговля» удалён из интерфейса;
- старый терминальный код изолирован и больше не запускается;
- Admin Panel использует одну форму готовой сделки;
- сделка сразу появляется в истории AI Assistant;
- результат рассчитывается от AI-баланса активного пользователя.

Установка:
1. Выполнить `supabase-ai-direct-trades.sql`.
2. Загрузить остальные файлы в GitHub с заменой.
3. `supabase-config.js` не заменять.


## Clean No Terminal Fix v1.6

Удалены последние исполняемые вызовы старого терминала:

- `renderOrders()`
- `stopTerminalRealtimeStream()`
- `updateTerminalBalances()`
- автоматический вызов `loadTradingTerminal()`

Также удалена оставшаяся логика режима `trading-mode`.

SQL повторно выполнять не нужно.


## Terminal Clean Fix 2.1

- старый терминал физически удалён из dashboard.js;
- новый терминал работает только через terminal.html / terminal.js;
- профиль, FASTBOOT ID, баланс и Admin Panel не зависят от терминала;
- терминал растянут на всю доступную область;
- исправлено подключение графика;
- добавлен резервный Binance API;
- терминал использует авторизованный Supabase-клиент родительской страницы;
- сохранена мобильная прокрутка окна пополнения;
- минимальный запуск AI Assistant — 50 USDT.

SQL повторно выполнять не нужно, если `supabase-terminal-v2.sql`
уже был успешно выполнен.


## Mobile Terminal Views 2.2

На мобильной версии:

- основная страница терминала показывает только стакан и форму открытия сделки;
- график открывается отдельной кнопкой «График»;
- в графике добавлена кнопка с домиком для возврата в терминал;
- график открывается на весь экран;
- на компьютере расположение терминала не изменено.

SQL выполнять не нужно.


## Desktop Terminal Home 2.3

Добавлено:

- кнопка с домиком в верхней панели терминала на компьютере;
- кнопка возвращает пользователя на Dashboard без выхода из аккаунта;
- мобильная кнопка возврата из графика сохранена;
- на телефоне компьютерная кнопка скрыта, чтобы не занимать место.

SQL выполнять не нужно.


## Dashboard Home Fix 2.4

Исправлено:

- кнопка возврата на Dashboard всегда видна и на телефоне, и на компьютере;
- на компьютере отображается домик с подписью Dashboard;
- на телефоне отображается компактная кнопка с домиком;
- сначала используется прямой вызов родительского Dashboard;
- добавлены резервные способы возврата, если iframe-сообщение не сработает;
- `openSection` явно доступна терминалу;
- обновлены версии файлов против старого кэша.

SQL выполнять не нужно.


## AI History Sync Fix

Исправлена причина, по которой сделки администратора не появлялись в AI Assistant:

- раньше система сравнивала дату сделки с датой запуска AI;
- теперь сделка применяется к пользователям, у которых AI активен в момент публикации;
- выбранная дата влияет только на отображение истории;
- добавлена синхронизация ранее пропущенных сделок;
- повторное начисление блокируется уникальной связкой user_id + trade_id;
- после публикации история AI принудительно перечитывается из Supabase.

### Установка

1. Выполните `supabase-ai-history-sync-fix.sql` в Supabase SQL Editor.
2. Загрузите остальные файлы в GitHub с заменой.
3. Не заменяйте `supabase-config.js`.


## Terminal Drawing Tools 2.5

Добавлено:

- компактная верхняя панель;
- удалён лишний чёрный отступ снизу;
- отступ справа от последней свечи;
- трендовая линия;
- горизонтальная линия;
- вертикальная линия;
- прямоугольная зона;
- текст на графике;
- отмена последнего объекта;
- очистка всех объектов;
- скрытие и отображение объёма;
- сброс масштаба;
- графические объекты сохраняются отдельно для каждой пары и таймфрейма;
- рисование работает мышью и пальцем.

SQL выполнять не нужно.


## Terminal Combined Fix 2.6

Эта версия объединяет:

- исправление `loadMarketUniverse is not defined`;
- компактную верхнюю панель терминала;
- удаление лишней чёрной полосы снизу;
- отступ справа от последней свечи;
- инструменты рисования;
- трендовую линию;
- горизонтальную и вертикальную линии;
- прямоугольные зоны;
- текст;
- отмену и очистку объектов;
- скрытие объёма;
- сброс масштаба.

Важно: предыдущий Market Universe Fix был собран поверх старой версии терминала,
поэтому оформление графика откатилось. В версии 2.6 оба обновления объединены.

SQL выполнять не нужно.


## FASTBOOT Terminal v3 Clean

Терминал полностью переписан с нуля.

Сохранено:

- отдельный торговый счёт;
- перевод между основным и торговым балансом;
- рыночные LONG/SHORT позиции;
- лимитные ордера;
- автоматическое исполнение лимитных ордеров;
- отмена ордеров;
- открытые позиции;
- история сделок;
- Binance котировки и стакан;
- Supabase RPC и таблицы текущей версии.

Добавлено:

- компактная верхняя панель;
- отсутствует нижняя чёрная полоса;
- отступ справа от последней свечи;
- трендовая линия;
- горизонтальная линия;
- вертикальная линия;
- прямоугольная зона;
- текст на графике;
- отмена и очистка объектов;
- скрытие объёма;
- сброс масштаба;
- отдельный мобильный режим графика;
- постоянная кнопка Dashboard;
- кнопки 25%, 50%, 75%, 100%.

SQL повторно выполнять не нужно, если `supabase-terminal-v2.sql`
уже был успешно выполнен.


## Terminal Integration v3.1

Добавлено:

- компактный блок «Торговый счёт / Основной баланс / Перевести» в одну строку;
- нижняя область терминала заполняет экран без чёрной полосы;
- Take Profit и Stop Loss для открытых позиций;
- автоматическое закрытие при достижении TP/SL;
- линии входа, TP, SL и лимитных ордеров на графике;
- сделки терминала загружаются в торговый журнал;
- Admin Panel показывает, у кого AI Assistant включён;
- обновление журнала после закрытия позиции.

Установка:

1. Выполнить `supabase-terminal-integration-v3-1.sql`.
2. Загрузить остальные файлы в GitHub с заменой.
3. Не заменять `supabase-config.js`.


## Layout Clean 3.2

Исправлено:

- кнопка Dashboard, логотип FASTBOOT, выбор пары и цена выровнены по одной линии;
- убрана подпись «Пара»;
- раздел «Торговый журнал» удалён из бокового меню;
- HTML-раздел торгового журнала удалён;
- сохранённый ранее раздел journal автоматически заменяется на Dashboard;
- обновлены версии файлов для сброса кэша.

SQL выполнять не нужно.


## Admin Panel Fix 3.3

Исправлена пустая Admin Panel.

Причина:
при удалении раздела «Торговый журнал» вместе с ним был случайно удалён
HTML-раздел Admin Panel, хотя JavaScript-логика осталась.

Исправлено:

- Admin Panel полностью восстановлена;
- торговый журнал остаётся удалённым;
- восстановлены пользователи, заявки и форма AI-сделок;
- добавлена колонка статуса AI Assistant;
- Admin Panel снова отображается при открытии;
- обновлены версии файлов против старого кэша.

SQL выполнять не нужно.


## Transfer Layout 3.4

Исправлено:

- верхний блок «Торговый счёт» полностью убран;
- кнопка «Перевести» перенесена рядом с доступным балансом;
- строка «Доступно / Перевести» адаптирована под мобильную версию;
- верхняя панель терминала стала компактнее;
- обновление скрытых старых элементов защищено от JavaScript-ошибок.

SQL выполнять не нужно.


## Mobile Price Clean 3.5

Исправлено:

- убрана текущая цена возле выбора торговой пары;
- убран процент изменения возле выбора торговой пары;
- верхняя мобильная панель теперь состоит только из:
  домика, FASTBOOT и выбора монеты;
- выбор монеты растягивается по оставшейся ширине;
- JavaScript безопасно работает после удаления элементов цены;
- обновлены версии terminal.html, terminal.css и terminal.js.

SQL выполнять не нужно.


## Safe UI Polish 3.6

Исправлено:

- у кнопки Dashboard убран домик;
- надпись Dashboard выровнена по центру;
- стакан сделан немного ниже;
- строки стакана уменьшены, чтобы блок не выступал за график;
- при запуске AI Assistant с балансом меньше 50 USDT показывается точное сообщение;
- в сообщении отображается текущий AI-баланс пользователя.

SQL выполнять не нужно.


## Desktop Without Order Book 3.7

Изменено:

- на компьютерной версии биржевой стакан скрыт;
- график расширен на всё свободное пространство;
- справа остаётся только окно открытия сделки;
- нижняя панель с позициями, открытыми ордерами и историей сделок сохранена;
- на мобильной версии стакан остаётся и работает как раньше;
- обновлены версии файлов против кэша.

SQL выполнять не нужно.


## Referral Program v1

Добавлено:

- раздел Referral в личном кабинете;
- уникальный код и ссылка пользователя;
- регистрация по `register.html?ref=FAST-...`;
- список прямых рефералов;
- активные и неактивные рефералы;
- общий, доступный и выплаченный доход;
- перевод реферального баланса на основной счёт;
- статусы START / SILVER / GOLD / PLATINUM;
- Admin Panel со статистикой и ТОП партнёров;
- начисление 5% от подтверждённой комиссии платформы;
- защита от повторного начисления через уникальный номер операции.

Установка:

1. Выполнить `supabase-referral-v1.sql` в Supabase SQL Editor.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.

Программа одноуровневая: вознаграждение начисляется только прямому
пригласившему пользователю.


## AI Fees + Referral Levels v2

Изменено:

- с Dashboard удалён блок «Данные аккаунта»;
- в AI Assistant добавлено прозрачное уведомление о комиссии 10%;
- комиссия удерживается только с положительной валовой прибыли;
- история AI показывает валовую прибыль, комиссию и чистый результат;
- AI помечен как demo / paper trading до подключения биржевого API;
- уровни Referral: START 5%, SILVER 15%, GOLD 25%, PLATINUM 40%;
- добавлено сообщение о дополнительных бонусах при повышении уровня;
- пользователь видит ID реферала, дату, активный баланс и свой доход;
- Admin Panel показывает уровень, ставку, число рефералов и доход партнёра;
- клик по партнёру открывает его рефералов;
- в таблице пользователей отображаются заработок и комиссии AI.

Установка:

1. Если Referral SQL ещё не выполнялся успешно, выполнить
   `supabase-referral-v1.sql`.
2. Выполнить `supabase-ai-fees-referral-v2.sql`.
3. Загрузить остальные файлы с заменой.
4. Не заменять `supabase-config.js`.


## AI Disclosure Clean 2.1

Изменено:

- удалены повторяющиеся надписи Demo / Paper Trading;
- оставлено одно короткое уведомление:
  «Сделки отображаются внутри платформы и не исполняются на внешней бирже»;
- информация о комиссии 10% сохранена;
- обновлены версии файлов против кэша.

SQL выполнять не нужно.


## Admin + Referral Fix 2.2

Исправлено:

- Admin Panel восстановлена;
- кнопка Admin Panel снова появляется у администратора;
- в пользовательском разделе Referral удалена колонка «Статус»;
- статус рефералов остаётся доступен администратору;
- таблица рефералов перестроена на четыре колонки;
- обновлены версии файлов против кэша.

SQL выполнять не нужно.

## Admin + Referral Income Fix 2.3
- Admin Panel restored.
- Referral active balance replaced with referral income.
- Added SQL migration for referral income.


## Admin Visible Fix 2.4

Исправлено:

- устранён конфликт `.hidden { display:none!important }`;
- кнопка Admin Panel показывается через inline style и CSS `!important`;
- роль очищается через trim и приводится к нижнему регистру;
- раздел Admin Panel принудительно отображается при открытии;
- добавлен совместимый SQL дохода рефералов без net_pnl_amount;
- добавлен файл проверки роли администратора.


## Balance Fix 2.5

Исправлено:

- Dashboard больше не зависит от прямого SELECT таблицы wallets;
- терминал использует тот же источник баланса;
- добавлена функция `get_my_wallet()` с security definer;
- отсутствующий кошелёк создаётся автоматически;
- загружаются spot, AI и trading балансы;
- запрос истории AI больше не ломается из-за отсутствующих новых колонок.

Установка:

1. Выполнить `supabase-balance-fix-v2-5.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Null Elements Fix 2.6

Исправлено:

- удалённые элементы Dashboard больше не вызывают `Cannot set properties of null`;
- `initializeUser()` безопасно проверяет существование каждого элемента;
- отображение Admin Panel сохранено;
- `renderAccount()` защищён от отсутствующих карточек;
- добавлен встроенный favicon, чтобы убрать 404 из консоли.

SQL выполнять не нужно.


## AI Commission Fix 2.8

Исправлено:

- комиссия составляет 10% только с прибыльной сделки;
- с убыточной сделки комиссия равна 0;
- старые сделки пересчитываются один раз;
- старые комиссии вычитаются из AI-баланса один раз;
- новые сделки сразу сохраняют валовую прибыль, комиссию и чистый результат;
- история AI показывает рассчитанную комиссию даже до перезагрузки страницы.

Установка:

1. Выполнить `supabase-ai-commission-fix-v2-8.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Mobile Horizontal Scroll Fix 2.9

Исправлено:

- широкие карточки Admin Panel можно прокручивать пальцем в сторону;
- горизонтальная прокрутка добавлена для статистики;
- таблица пользователей больше не обрезается;
- финансовые заявки можно листать по горизонтали;
- таблицы партнёров и рефералов прокручиваются;
- история AI-сделок прокручивается;
- содержимое модальных окон адаптировано под мобильный экран;
- добавлена подсказка «Проведите в сторону» у широких таблиц;
- основной экран больше не растягивается шире viewport.

SQL выполнять не нужно.


## Admin User Profit + Fees 3.0

Исправлено:

- в Admin Panel считается чистый заработок каждого пользователя;
- комиссия считается как 10% с каждой прибыльной AI-сделки;
- убыточные сделки не создают комиссию;
- старые сделки получают рассчитанные поля gross / fee / net;
- баланс пользователей повторно не списывается;
- frontend поддерживает резервные названия полей RPC.

Установка:

1. Выполнить `supabase-admin-user-profit-fees-v3-0.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Withdrawal Rules + Mobile Terminal Tabs 3.1

Добавлено:

- вывод не чаще одного раза в 14 дней;
- минимальный вывод 50 USDT;
- доступная сумма состоит из невыведенной чистой AI-прибыли
  и доступных Referral-бонусов;
- первоначальный вклад не входит в доступный вывод;
- на Dashboard добавлена карточка доступной суммы;
- в окне вывода показывается разбивка AI / Referral;
- сервер проверяет сумму, период и источник средств;
- при одобрении списывается AI-прибыль и Referral-баланс;
- на мобильной версии терминала возвращены:
  Позиции, Открытые ордера и История сделок.

Установка:

1. Выполнить `supabase-withdrawal-rules-v3-1.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Spot Withdrawal Rules 3.2

Изменено:

- доступно к выводу = баланс основного счёта;
- ограничение только на прибыль AI и Referral удалено;
- минимальный вывод остаётся 50 USDT;
- новая заявка доступна один раз в 14 дней;
- в Dashboard и окне вывода показывается основной баланс;
- при одобрении средства списываются с основного счёта.

Установка:

1. Выполнить `supabase-spot-withdrawal-rules-v3-2.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Terminal Balance Dashboard 3.3

Добавлено:

- карточка «Баланс терминала» на Dashboard;
- значение берётся из `wallets.trading_balance`;
- общий баланс включает основной счёт, AI Bot и терминал;
- терминальный баланс добавлен в портфель и распределение;
- адаптивная сетка для пяти карточек.

SQL выполнять не нужно.


## Terminal Balance Hard Fix 3.4

Исправлено:

- добавлена версия к `dashboard-auth.js`, а не только к `dashboard.js`;
- добавлены no-cache meta tags;
- карточка терминала создаётся JavaScript-кодом, даже если загружен старый HTML;
- значение берётся из `trading_balance`;
- добавлен build marker `FASTBOOT_BUILD_VERSION`;
- сетка карточек принудительно обновлена.

После загрузки в Network должен быть файл:
`dashboard.js?v=terminal-balance-hard-3-4`.


## Admin User Full Statistics 3.5

Добавлено:

- имя пользователя в Admin Panel стало кликабельным;
- отдельное большое окно полной статистики;
- общий, основной, AI и терминальный балансы;
- пополнения и выводы;
- переводы между счетами;
- все системные операции;
- позиции, ордера и история терминала;
- сделки AI с валовой прибылью, комиссией и чистым результатом;
- статистика аккаунта и AI-бота;
- адаптация окна под мобильную версию.

Установка:

1. Выполнить `supabase-admin-user-full-statistics-v3-5.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Admin User Details + Bot Toggle 3.6

Исправлено:

- окно полной статистики больше не падает, если отсутствуют таблицы терминала;
- данные возвращаются частично и безопасно;
- добавлена кнопка включения/выключения AI-бота;
- статус обновляется сразу в модальном окне и в списке пользователей.

Установка:

1. Выполнить `supabase-admin-user-details-bot-toggle-v3-6.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Admin User Split RPC 3.7

Исправлено:

- удалена зависимость от одной большой SQL-функции;
- профиль, финансы, переводы, операции, терминал, AI и Referral
  загружаются независимо;
- ошибка одной секции больше не оставляет всё окно пустым;
- добавлена отсутствовавшая функция `formatQuantity`;
- переключатель AI-бота работает через отдельный RPC;
- используются правильные поля `created_at`, `started_at` и `stopped_at`.

Установка:

1. Выполнить `supabase-admin-user-split-rpc-v3-7.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## AI History Date + Sort 3.8

Изменено:

- в пользовательской истории AI удалены отдельные колонки
  времени открытия и закрытия;
- оставлена одна колонка «Дата» без времени;
- последняя добавленная сделка отображается первой;
- сортировка выполняется по `created_at`, затем по резервным датам;
- в Admin Panel история AI также показывает только дату;
- добавлена frontend-сортировка как дополнительная защита.

Установка:

1. Выполнить `supabase-ai-history-date-sort-v3-8.sql`.
2. Загрузить остальные файлы с заменой.
3. Не заменять `supabase-config.js`.


## Terminal Top 100 + Mobile Space Fix 3.9

Добавлено:

- топ-100 USDT-пар загружается автоматически по объёму торгов за 24 часа;
- исключены leveraged-токены UP, DOWN, BULL и BEAR;
- поле торговой пары стало поисковым;
- поиск поддерживает ввод BTCUSDT, BTC/USDT и BTC-USDT;
- при недоступности API используется резервный список.

Исправлено на мобильной версии:

- удалена фиксированная высота рабочей области;
- нижние вкладки поднимаются сразу под стакан и форму сделки;
- убрано большое пустое пространство;
- позиции, ордера и история сделок остаются горизонтально прокручиваемыми.

SQL выполнять не нужно.
